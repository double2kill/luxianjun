<!--index.wxml-->
<view class="container">
  <view
    wx:for="{{paths}}" 
    wx:key="index"
    class="naviButton"
    bindtouchstart="naviToNavi"
    data-points="{{item.points}}"
  >
    {{item.name}}
  </view>
</view>
