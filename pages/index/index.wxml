<!--index.wxml-->
<view class="container">
  <view
    wx:for="{{paths}}" 
    wx:key="index"
    class="naviButton"
    bindtouchstart="naviToNavi"
    data-path_id="{{item.id}}"
  >
    {{item.name}}
  </view>
</view>
